strategy st1
{
   variables
   {
      BALL_PASS = rangeint(0,5),
      SHOT_ON_GOAL = rangeint(0,2, f(a)),
      INTERCEPT_BALL = rangebool(),
      DO_LACK = rangebool(f(b)),
      RECEIVE_BALL = rangefloat(0.1, 0.4),
      THROW_IN = rangefloat(0.3, 0.9),
      CORNER_KICK = rangefloat(1.0, 2.2, f(c)),
      CLEAR_BALL = rangechoice([1,2,3,4]),
      COMMIT_FOUL = rangechoice([1,2,3,4], f(c))
   };
   
   execute(stat_game)
   {
      int a = 1;
      int c = a + 2;
   };
};
