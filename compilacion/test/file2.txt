int b = p1.age;
team t1 = TEAM.Barcelona;
bool y = NOT x < 20;

function int increment():
{
   return 1 + 1;
   return (1 + 1);
};

int g = increment();

bool t = NOT (x >= 0 AND x >= 2);

b = len(players);
int c = len(t.players);
filter players by (_.age > 20);
