player p1 = (name="Messi", age=32, pos="DEL");
int a = 2;
int b = -1;
int c = a + b + 30;
float prob = 0.1;
print(prob);

if c >= 30:
{
  float prob1 = 0.2;
};

function float aumentar(float prob, float value):
{
  float new_prob = prob + value;
  if new_prob >= 0 AND new_prob <= 1:
  {
     return new_prob;
  };
  return 0;
};

float result = aumentar(prob, 0.3);
print(result);

bool gol = true;
str name = "Ronaldo";
