function float write_console():
{
  print ("Probando");
};

player p1 = (name="Messi,...probando", age=32, pos="DEL", list_prob=[0.8, 0.1, 0.85, 0.1, 0.05, 0.1, 0.6, 0.69, 0.2, 0.1, 0.01, 0.3], country="Espanna");
int a = 2;
int b = -1;
int c = a + b + 30;
float prob = 0.1;
print(prob);
print(p1);

if c >= 30:
{
  float prob1 = 0.2;
};

function float aumentar(float prob, float value):
{
  float new_prob = prob + value;
  if new_prob >= 0 AND new_prob <= 1:
  {
     return new_prob;
  };
  return 0;
};

write_console();
float result = aumentar(prob, 0.3);
print(result);

bool gol = true;
str name = "Ronaldo";
